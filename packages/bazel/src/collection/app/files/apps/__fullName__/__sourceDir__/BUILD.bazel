package(default_visibility = ["//visibility:public"])

load("@angular//:index.bzl", "ng_module")

ng_module(
    name = "<%= sourceDir %>",
    srcs = glob(["*.ts"]),
    tsconfig = ":tsconfig.json"
)
